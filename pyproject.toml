[project]
name = "elastic-trajectory-index-layout"
version = "0.0.1"
description = "Scaffold: Trajectory Index Layout (independent Elastic lens)"
requires-python = ">=3.11"
dependencies = []

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
